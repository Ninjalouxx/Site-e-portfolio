/* Reset CSS */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body styling */
body {
    font-family: Arial, sans-serif;
    background-color: #ebe3da;
}

/* Welcome screen styling */
.welcome-screen {
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    text-align: center;
}

.welcome-screen img {
    width: 100%;
    height: auto;
}

.welcome-text {
    position: absolute;
}

.welcome-text h1 {
    font-size: 2.5rem;
    margin-bottom: 10px;
}

.welcome-text h2 {
    font-size: 2rem;
}

/* Menu styling */
/* Menu styling */
.menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    background-color: #6D454C;
    text-align: center;
    z-index: 1000; /* Assurez-vous que le menu reste au-dessus du reste du contenu */
}

.menu ul {
    list-style: none;
}

.menu ul li {
    display: inline;
    margin: 0 10px;
}

.menu ul li a {
    color: white;
    text-decoration: none;
}


/* Section styling */
.section {
    padding: 50px 20px;
}

.section h2 {
    font-size: 2rem;
    margin-bottom: 20px;
	color: #6D454C;
}

/* Section A propos de moi styling */
#about {
    margin-top: 150px;
}

#experience {
    margin-top: 50px;
}

#skills {
    margin-top: 50px;
}

#projects {
    margin-top: 50px;
}

#interests {
    margin-top: 50px;
}

#contact {
    margin-top: 50px;
}

/* Menu boutons styling */
.menu ul li {
    display: inline;
    margin: 0 0 100 100 px;
    vertical-align: top; /* Alignement vertical vers le haut */
}

/* Centrage des titres de section */
.section h1 {
    font-size: 2rem;
    margin-bottom: 20px;
    text-align: center; /* Ajout de cette propriété pour centrer le texte */
}

/* Mise en forme de la section "A propos de moi" */
#about {
    text-align: center;
    padding: 50px 0;
}

#about h2 {
    text-align: left;
    font-size: 24px;
    font-weight: bold;
}

#about p {
    font-size: 18px;
}

#about img {
    margin-top: 20px; /* Ajuster la marge vers le bas */
    max-width: 300px; /* Ajuster la largeur maximale */
    width: 100%; /* Ajuster la largeur pour remplir la colonne */
    height: auto; /* Permettre le redimensionnement proportionnel */
}

/*Parcours*/
.timeline {
    position: relative;
    margin-top: 30px;
    padding: 0;
    list-style: none;
	
}

.timeline-item {
    display: flex;
    align-items: flex-start;
    margin-bottom: 40px;
	background-color: #86BBD2;
}

.timeline-marker {
    width: 20px;
    height: 20px;
    background-color: #86BBD2;
    border-radius: 50%;
    margin-right: 20px;
    flex-shrink: 0;
    position: relative;
    top: 12px;
}

.timeline-content {
    background-color: #86BBD2;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    flex-grow: 1;
}

.timeline-description {
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    flex-basis: 300px; /* Largeur des descriptions */
}

/* Styles pour les compétences */
.skills-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    margin-top: 30px;
}

.skill {
    width: calc(17.5% - 20px); /* Allonger les blocs à 1/3 de la largeur de l'écran avec un espacement de 20px */
    background-color: #b0c2bd;
    border-radius: 8px;
    margin-bottom: 20px;
    display: flex;
    flex-direction: column; /* Mettre le contenu en colonne */
}

.skill-content {
    padding: 20px;
    flex-grow: 1; /* Faire en sorte que le contenu occupe tout l'espace disponible en hauteur */
}

.skill-content p {
    margin: 0;
    color: black;
}

.skill-image {
    padding: 10px;
    text-align: center;
}

.skills-container .skill:nth-child(even) {
    transform: translateY(50px);
}

.skills-description {
    text-align: center;
    margin-top: 50px;
    position: relative;
}

.skills-description p {
    background-color: #ebe3da;
    border-radius: 8px;
    padding: 20px;
    color: black;
    margin-top: 20px; /* Espacement par rapport au trait */
}




/* Styles pour les projets personnels */
.projects-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    margin-top: 30px; /* Augmenter l'espace entre le titre et les blocs */
}

.project {
    width: calc(33.33% - 20px); /* Utiliser 1/3 de la largeur de l'écran avec un espacement de 20px */
    background-color: #b0c2bd;
    border-radius: 8px;
    margin-bottom: 20px;
}

.project h3 {
	text-align: center;
    margin-top: 0;
    color: black;
}

.project-image {
    text-align: center;
    margin-bottom: 10px;
}

.project-description p {
	text-align: center;
    margin-bottom: 5px;
    color: black;
}

/* Styles pour les centres d'intérêts */
.interests-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    margin-top: 30px; /* Espacement par rapport au titre */
}

.interest {
    width: calc(33.33% - 20px); /* Utiliser 1/3 de la largeur de l'écran avec un espacement de 20px */
    background-color: #b0c2bd;
    border-radius: 8px;
    margin-bottom: 20px;
}

.interest h3 {
    text-align: center;
    margin-top: 0;
    color: black;
}

.interest-image {
    text-align: center;
    margin-bottom: 10px;
}

.interest-description p {
    text-align: center;
    margin-bottom: 5px;
    color: black;
}

/* Styles pour la section de contact */




h2 {
    text-align: center;
    margin-bottom: 30px;
    color: #333;
}

.form-group {
    margin-bottom: 20px;
}

label {
    display: block;
    font-weight: bold;
    margin-bottom: 5px;
}

input[type="email"],
textarea {
    width: calc(100% - 22px);
    padding: 10px;
    font-size: 16px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

textarea {
    resize: vertical;
}

button[type="submit"] {
    background-color: #007bff;
    color: #fff;
    border: none;
    padding: 10px 20px;
    font-size: 16px;
    border-radius: 5px;
    cursor: pointer;
}

button[type="submit"]:hover {
    background-color: #0056b3;
}

/* Styles pour les informations de contact */
.contact-info {
    display: flex;
    align-items: center; /* Centre verticalement les éléments */
    margin-top: 20px; /* Ajustez selon vos besoins */
}

.contact-info-left img {
    width: 50px; /* Ajustez la taille de l'image selon vos préférences */
    height: auto;
    margin-right: 10px; /* Ajoute une marge à droite de l'image */
}

.contact-info-right p {
    font-size: 18px;
    margin-top: 0; /* Supprime la marge supérieure du paragraphe */
    margin-left: 10px; /* Ajoute une marge à gauche du texte */
}

